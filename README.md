# Media_Ratings_Platform
Standalone application in Java that implements a RESTful HTTP server that acts as an API for possible frontends (e.g., Mobile, Web, console). The front end is not part of this project!
